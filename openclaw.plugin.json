{
  "id": "agentic-rag",
  "name": "Agentic RAG",
  "description": "Bridge plugin that exposes confidence-gated Python RAG as an OpenClaw tool.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "pythonBin": {
        "type": "string",
        "description": "Python binary used to execute the bridge script."
      },
      "workingDir": {
        "type": "string",
        "description": "Optional plugin-relative working directory for bridge execution."
      },
      "bridgeScript": {
        "type": "string",
        "description": "Plugin-relative path to bridge script.",
        "default": "bridge/run_agentic_rag_tool.py"
      },
      "retrievalMode": {
        "type": "string",
        "enum": [
          "lexical",
          "hybrid"
        ],
        "default": "lexical",
        "description": "Retrieval mode for Python core. Use hybrid to enable lexical+semantic fusion."
      },
      "embeddingEnabled": {
        "type": "boolean",
        "default": false,
        "description": "Enable embedding-backed semantic retrieval stream."
      },
      "embeddingBaseUrl": {
        "type": "string",
        "description": "OpenAI-compatible embeddings base URL, e.g. http://127.0.0.1:1234/v1."
      },
      "embeddingModel": {
        "type": "string",
        "description": "Embedding model id used by embedding endpoint."
      },
      "embeddingTimeoutMs": {
        "type": "integer",
        "minimum": 500,
        "maximum": 120000,
        "default": 10000
      },
      "hybridLexicalWeight": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "default": 0.35,
        "description": "Weight of lexical score in hybrid fusion. Semantic weight is (1 - lexical)."
      },
      "hybridMinLexicalScore": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "default": 0.0,
        "description": "Optional lexical floor for hybrid hits. Semantic-only matches below this lexical score are dropped."
      },
      "corpusPath": {
        "type": "string",
        "description": "Path to retrieval source (JSON corpus file or folder with txt/md/pdf/odt files)."
      },
      "minRetrievalScore": {
        "type": "number",
        "minimum": 0,
        "maximum": 1
      },
      "minConfidence": {
        "type": "number",
        "minimum": 0,
        "maximum": 1
      },
      "topK": {
        "type": "integer",
        "minimum": 1,
        "maximum": 32
      },
      "maxContextChars": {
        "type": "integer",
        "minimum": 64,
        "maximum": 50000
      },
      "abstainMessage": {
        "type": "string"
      },
      "arbiterEnabled": {
        "type": "boolean",
        "default": false,
        "description": "Enable optional three-role arbitration pass (evidence/action/policy)."
      },
      "arbiterSharedLabel": {
        "type": "string",
        "default": "contracts_v1",
        "description": "Shared label attached to all arbitration roles in diagnostics."
      },
      "arbiterMinEvidenceChars": {
        "type": "integer",
        "minimum": 16,
        "maximum": 20000,
        "default": 120,
        "description": "Minimum evidence chars required by evidence role before answering."
      },
      "arbiterHighImpactMargin": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "default": 0.1,
        "description": "Extra confidence margin required for high-impact intents."
      },
      "arbiterAllowRefine": {
        "type": "boolean",
        "default": true,
        "description": "Allow action role to recommend refine_query before answer."
      },
      "arbiterFailClosedOnConflict": {
        "type": "boolean",
        "default": true,
        "description": "If roles disagree, force abstain to fail closed."
      },
      "timeoutMs": {
        "type": "integer",
        "minimum": 1000,
        "maximum": 120000,
        "default": 15000
      }
    }
  }
}
